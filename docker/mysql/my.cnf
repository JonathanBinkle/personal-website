# /etc/mysql/my.cnf

[mysqld]
# Disable access to local filesystem
local-infile=0

# Enable general, slow (>= 10 seconds), and error logs
general_log=1
general_log_file=/var/log/mysql/general.log
slow_query_log=1
slow_query_log_file=/var/log/mysql/slow-query.log
log_error=/var/log/mysql/error.log
